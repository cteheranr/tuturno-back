import { Role } from "../enums/roles.enum";

export const CRUDAdmin = [ Role.Administrator, Role.SUPERADMIN ];
export const rep = [Role.recepcionista]
export const allPersonal = [ Role.Administrator, Role.SUPERADMIN, Role.operador, Role.recepcionista ];
